pipeline {
  agent any

  environment {
    AWS_REGION            = 'us-east-1'
    STACK_NAME            = 'MyCloudFormationStack'
    TEMPLATE_FILE         = 'aws-templates/infra.json'
  }

  stages {
    stage('Checkout') {
      steps {
        git url: 'https://github.com/naveenkumarpsnk/aws-terraform.git', branch: 'main'
      }
    }

    stage('Deploy CloudFormation') {
      steps {
        withCredentials([awsCredentials(credentialsId: 'aws_cred')]) {
          script {
            // Ensure the AWS CLI is configured
            sh """
          aws cloudformation deploy \
            --stack-name ${STACK_NAME} \
            --template-file ${TEMPLATE_FILE} \
            --capabilities CAPABILITY_NAMED_IAM
        """
          }
        }
      }
    }
  }

  post {
    success {
      echo '✅ CloudFormation stack deployed successfully!'
    }
    failure {
      echo '❌ Deployment failed.'
    }
  }
}
